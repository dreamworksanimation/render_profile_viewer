# .readthedocs.yml

formats: []  # Don't build htmlzip, pdf or epub

build:
  image: latest

requirements_file: docs/requirements.txt

python:
  version: 2.7
  setup_py_install: true
  pip_install: false
